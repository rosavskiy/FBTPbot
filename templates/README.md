# Шаблоны для работы с базой знаний

## import_template.json

Шаблон для импорта Q&A пар в базу знаний.

### Использование

1. Скопируйте `import_template.json`
2. Замените данные на свои
3. Загрузите через KB Admin (📥 Импорт данных) или API

### Обязательные поля

| Поле | Описание |
|------|----------|
| `id` | Уникальный идентификатор (например: `custom_001`) |
| `text` | Полный текст: `"Вопрос: ...\n\nОтвет: ..."` |
| `metadata.question` | Текст вопроса |
| `metadata.answer` | Текст ответа |

### Опциональные поля metadata

| Поле | По умолчанию | Описание |
|------|--------------|----------|
| `category` | "Прочее" | Категория на русском |
| `category_en` | "general" | Категория на английском |
| `tags` | [] | Массив тегов |
| `quality_score` | 3 | Оценка качества (1-5) |
| `source` | "manual_import" | Источник данных |
| `type` | "qa_pair" | Тип записи |

### Доступные категории

- Прочее (general)
- Работа с документами (documents)
- Обучение (training)
- Маркировка (marking)
- Проблема с кассой (cashier)
- Отчётность (reports)
- Проблема с оборудованием (hardware)
- Инвентаризация (inventory)
- Система заказов (orders)
- Маркетинг (marketing)
- Скидки (discounts)

### Импорт через API

```bash
curl -X POST "http://YOUR_SERVER:8000/api/kb/import" \
  -F "file=@your_qa_pairs.json"
```

### Примечания

- ID должны быть уникальными — дубликаты пропускаются
- После импорта данные сразу доступны боту
- Создаётся автоматический бэкап
